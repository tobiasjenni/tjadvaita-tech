#!/usr/bin/env python3
"""Retired: this public repository deploys static files to GitHub Pages.

The former analytics server is deliberately disabled. Do not run historical
versions; they contained an unsafe fallback credential and rendered request
metadata as HTML. Any separately deployed historical copy needs to be retired
or fixed and its access credential replaced on that server.
"""
raise SystemExit("Analytics server retired. This repository is a static website.")
